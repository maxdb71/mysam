import {WorkOrderControlsLibrary as LibWoControls} from '../WorkOrderLibrary';

export default function WorkOrderCreateUpdateZSystemCondn(pageProxy) {
    return LibWoControls.getZSystemCondn(pageProxy);
}
